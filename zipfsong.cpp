#include <iostream>
#include <unordered_map>
#include <vector>
using namespace std;

int main () {
	int songs, picks;
	int plays, count, quality, ref;
	string names;
	map<int, string> list;

	cin >> songs >> picks;

	for (int i = 0; i < songs; i++) {
		count = i+1;
		cin >> plays >> names;
		if (!i) ref = plays;
		quality = plays/(ref/count);
		list[]
	}

	for (int i = 0; i < songs; i++) {

	}

}